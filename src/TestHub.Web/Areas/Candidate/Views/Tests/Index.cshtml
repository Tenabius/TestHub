@using TestHub.Core.Extensions;
@model IList<TestInfoViewModel>

<div class="row row-cols-4">
    @foreach(var testInfo in Model)
    {
        <div class="col card">
            <div class="card-content">
                <h4 class="card-title">
                    <a asp-area="Candidate" asp-controller="Tests" asp-action="TestInfo" asp-route-id="@testInfo.Id.ShortGuid()">
                        @testInfo.Title
                    </a>
                </h4>
                <p class="">
                    @testInfo.Description
                </p>
            </div>
            <div class="card-read-more">
                <a asp-area="Candidate" asp-controller="Tests" asp-action="TestInfo" asp-route-id="@testInfo.Id.ShortGuid()">
                    Pass test
                </a>
            </div>
        </div>
    }
</div>